buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:2.0.1'
        
    }
}

plugins {
    id 'com.github.johnrengelman.shadow' version '2.0.1'
    id 'java'
    id 'application'
    id 'idea'
    id "org.sonarqube" version "3.0"
  
}

repositories {
    jcenter()
}

/*task customFatJar(type: Jar){
    manifest {
        attributes 'Main-Class' : 'com.newrelic.codingchallenge.ServerStart'
     }
     baseName = 'MainFat'
     from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
     with jar
   } */



dependencies {
    // Dependencies go here
    compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.8'
     //classpath "org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:3.0"
    testCompile 'junit:junit:4.12'
}
apply plugin: "org.sonarqube"
mainClassName = 'com.newrelic.codingchallenge.ServerStart'

shadowJar {
    baseName = 'coding-challenge-shadow'
    classifier = null
    version = null
    manifest {
        attributes 'Description': 'New Relic coding challenge application JAR'
    }
}
